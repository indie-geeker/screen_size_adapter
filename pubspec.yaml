name: screen_size_adapter
description: Root-viewport screen-size adaptation for Flutter using standard bindings.
version: 2.0.0-dev.1
homepage: https://github.com/indie-geeker/screen_size_adapter
repository: https://github.com/indie-geeker/screen_size_adapter
issue_tracker: https://github.com/indie-geeker/screen_size_adapter/issues
topics:
  - screen-size
  - layout
  - design-size
  - responsive

environment:
  sdk: ^3.7.2
  flutter: ">=3.29.2"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
