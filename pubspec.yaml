name: screen_size_adapter
description: Binding-level screen-size adapter for Flutter with per-view configuration.
version: 0.3.0
homepage: https://github.com/indie-geeker/screen_size_adapter
repository: https://github.com/indie-geeker/screen_size_adapter
issue_tracker: https://github.com/indie-geeker/screen_size_adapter/issues
topics:
  - screen-size
  - multi-view
  - layout
  - design-size
  - responsive

environment:
  sdk: ^3.7.2
  flutter: ">=3.29.2"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
